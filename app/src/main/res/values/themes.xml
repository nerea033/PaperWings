<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PaperWings" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/brown_background</item>
        <item name="colorSecondary">@color/brown_background1</item>
    </style>

    <style name="Theme.PaperWings" parent="Base.Theme.PaperWings" />

    <!-- Color del indicador del botton navigation menú -->
    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/brown_background3</item>
    </style>
</resources>